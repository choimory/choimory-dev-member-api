spring:
  config:
    activate:
      on-profile: dev

  jpa:
    show-sql: true
    generate-ddl: false
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        show_sql: true
        format_sql: true


logging:
  level:
    root: error
    dev.choimory: info
    org:hibernate.SQL: error